import React, { Component } from 'react';
import { connect } from 'react-redux';

class MarkdownOptions extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (

    );
  }
}

const mapStateToProps = state => {
  return {

  };
};

export default connect(mapStateToProps, mapDispatchToProps)(MarkdownOption);
